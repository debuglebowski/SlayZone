# Tags Domain (Renderer)

Tag management UI components.

## Responsibilities

- Tag CRUD (inline in task metadata)
- Tag selection for tasks
- Tag color picker

## Structure

Currently minimal - tag UI is integrated into `task/TaskMetadataSidebar`.

## Dependencies

- `../task` - Consumed by task metadata sidebar
