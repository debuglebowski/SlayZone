{
  "name": "@slayzone/website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm build:og && astro build && pnpm check:links",
    "dev": "astro dev",
    "preview": "astro preview",
    "build:og": "node scripts/build-og.mjs",
    "check:links": "node scripts/check-links.mjs",
    "check:urls": "node scripts/check-urls.mjs"
  },
  "devDependencies": {
    "astro": "^5.14.3",
    "@types/js-yaml": "^4.0.9"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.7.1",
    "js-yaml": "^4.1.1"
  }
}
