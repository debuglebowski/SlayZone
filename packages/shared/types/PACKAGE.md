# @omgslayzone/types

ElectronAPI contract shared between main and renderer processes.

## Exports

- `ElectronAPI` - Type definition for `window.api` exposed via preload

## Dependencies

Imports types from all domains to compose the full API contract:
- terminal, task, projects, tags, settings, worktrees
