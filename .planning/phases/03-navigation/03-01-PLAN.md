---
phase: 03-navigation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/ui/sidebar.tsx
  - src/renderer/src/components/ui/context-menu.tsx
  - src/renderer/src/components/ui/dropdown-menu.tsx
  - src/renderer/src/components/ui/separator.tsx
  - src/renderer/src/components/ui/color-picker.tsx
  - package.json
autonomous: true

must_haves:
  truths:
    - "shadcn Sidebar, ContextMenu, DropdownMenu, Separator components available"
    - "react-colorful installed and wrapped in color-picker component"
    - "Color picker shows hue/saturation picker + hex input"
  artifacts:
    - path: "src/renderer/src/components/ui/sidebar.tsx"
      provides: "Sidebar component"
      contains: "SidebarProvider"
    - path: "src/renderer/src/components/ui/context-menu.tsx"
      provides: "Right-click menu"
      contains: "ContextMenuTrigger"
    - path: "src/renderer/src/components/ui/color-picker.tsx"
      provides: "Color picker wrapper"
      contains: "HexColorPicker"
  key_links:
    - from: "src/renderer/src/components/ui/color-picker.tsx"
      to: "react-colorful"
      via: "import"
      pattern: "from 'react-colorful'"
---

<objective>
Install shadcn sidebar/menu components and react-colorful. Create color-picker wrapper.

Purpose: Foundation components for all navigation UI
Output: UI primitives ready for sidebar + dialogs
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-navigation/03-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install shadcn components + react-colorful</name>
  <files>package.json, src/renderer/src/components/ui/*.tsx</files>
  <action>
Run from project root:
```bash
npx shadcn@latest add sidebar context-menu dropdown-menu separator
npm install react-colorful
```

shadcn will create files in src/renderer/src/components/ui/. Verify all four components installed.
  </action>
  <verify>`ls src/renderer/src/components/ui/` shows sidebar.tsx, context-menu.tsx, dropdown-menu.tsx, separator.tsx. `npm ls react-colorful` shows installed.</verify>
  <done>Components installed, package.json updated</done>
</task>

<task type="auto">
  <name>Task 2: Create color-picker wrapper component</name>
  <files>src/renderer/src/components/ui/color-picker.tsx</files>
  <action>
Create `src/renderer/src/components/ui/color-picker.tsx`:

```tsx
import { HexColorPicker, HexColorInput } from "react-colorful"

interface ColorPickerProps {
  value: string
  onChange: (color: string) => void
}

export function ColorPicker({ value, onChange }: ColorPickerProps) {
  return (
    <div className="space-y-2">
      <HexColorPicker color={value} onChange={onChange} />
      <HexColorInput
        color={value}
        onChange={onChange}
        prefixed
        className="w-full px-3 py-2 border rounded-md text-sm"
      />
    </div>
  )
}
```

This wraps react-colorful with consistent styling.
  </action>
  <verify>File exists with HexColorPicker import. `npm run typecheck` passes (or no type errors in IDE).</verify>
  <done>ColorPicker component available at @/components/ui/color-picker</done>
</task>

</tasks>

<verification>
1. `ls src/renderer/src/components/ui/ | grep -E "(sidebar|context-menu|dropdown-menu|separator|color-picker)"` shows all 5 files
2. `npm run dev` starts without errors
3. Imports work: can add `import { Sidebar } from "@/components/ui/sidebar"` without error
</verification>

<success_criteria>
- shadcn Sidebar, ContextMenu, DropdownMenu, Separator installed
- react-colorful in package.json
- ColorPicker wrapper component created
- No build/type errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-navigation/03-01-SUMMARY.md`
</output>
