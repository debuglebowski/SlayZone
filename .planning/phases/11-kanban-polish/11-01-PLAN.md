---
phase: 11-kanban-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/renderer/src/components/sidebar/AppSidebar.tsx
autonomous: true

must_haves:
  truths:
    - "Settings button visible in sidebar footer"
    - "Tutorial button visible in sidebar footer"
    - "Clicking Settings opens settings dialog"
    - "Clicking Tutorial opens tutorial dialog"
    - "No dropdown menu in sidebar footer"
  artifacts:
    - path: "src/renderer/src/components/sidebar/AppSidebar.tsx"
      provides: "Two icon buttons with tooltips"
      contains: "Settings"
  key_links:
    - from: "Settings button onClick"
      to: "onSettings prop"
      via: "direct handler"
    - from: "Tutorial button onClick"
      to: "onTutorial prop"
      via: "direct handler"
---

<objective>
Replace dropdown menu with dedicated Settings and Tutorial icon buttons in sidebar footer.

Purpose: Direct button access is faster than dropdown (one click vs two).
Output: AppSidebar with two icon buttons instead of dropdown menu.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/11-kanban-polish/11-RESEARCH.md
@src/renderer/src/components/sidebar/AppSidebar.tsx
@src/renderer/src/components/ui/button.tsx
@src/renderer/src/components/ui/tooltip.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace dropdown with icon buttons</name>
  <files>src/renderer/src/components/sidebar/AppSidebar.tsx</files>
  <action>
1. Remove DropdownMenu imports (DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger)
2. Add imports: Settings, HelpCircle from lucide-react
3. Add imports: Button from @/components/ui/button
4. Add imports: Tooltip, TooltipTrigger, TooltipContent from @/components/ui/tooltip

5. Replace SidebarFooter content. Current:
   - DropdownMenu with trigger button and two menu items

   New structure:
   ```tsx
   <SidebarFooter className="py-4">
     <SidebarMenu>
       <SidebarMenuItem className="flex justify-center gap-2">
         <Tooltip>
           <TooltipTrigger asChild>
             <Button
               variant="ghost"
               size="icon-lg"
               onClick={onSettings}
               className="rounded-lg text-muted-foreground"
             >
               <Settings className="size-5" />
             </Button>
           </TooltipTrigger>
           <TooltipContent side="right">Settings</TooltipContent>
         </Tooltip>
         <Tooltip>
           <TooltipTrigger asChild>
             <Button
               variant="ghost"
               size="icon-lg"
               onClick={onTutorial}
               className="rounded-lg text-muted-foreground"
             >
               <HelpCircle className="size-5" />
             </Button>
           </TooltipTrigger>
           <TooltipContent side="right">Tutorial</TooltipContent>
         </Tooltip>
       </SidebarMenuItem>
     </SidebarMenu>
   </SidebarFooter>
   ```

Note: size="icon-lg" = size-10 matches existing w-10 h-10 buttons in sidebar.
Note: Tooltip component already wraps TooltipProvider internally, no root provider needed.
  </action>
  <verify>npm run typecheck passes</verify>
  <done>Sidebar footer shows two icon buttons (Settings gear, Tutorial question mark) instead of dropdown</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Settings and Tutorial buttons in sidebar footer</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Look at sidebar footer (bottom of left sidebar)
    3. Verify: Two icon buttons visible (gear and question mark)
    4. Hover each button - tooltip appears ("Settings", "Tutorial")
    5. Click Settings button - settings dialog opens
    6. Click Tutorial button - tutorial/onboarding dialog opens
    7. Verify: Buttons same size as other sidebar items (40x40px)
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
- No dropdown menu in sidebar footer
- Two icon buttons visible
- Both buttons functional (trigger correct dialogs)
- Tooltips show on hover
- Visual consistency with other sidebar buttons
</verification>

<success_criteria>
- KAN-01: Settings accessible via dedicated button (not menu) - DONE
- KAN-02: Tutorial accessible via dedicated button (not menu) - DONE
</success_criteria>

<output>
After completion, create `.planning/phases/11-kanban-polish/11-01-SUMMARY.md`
</output>
